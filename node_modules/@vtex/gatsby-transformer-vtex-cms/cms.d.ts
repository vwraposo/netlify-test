export interface Meta {
    slug: string;
    title?: string;
    description?: string;
}
export interface Block {
    name: string;
    props: Record<string, unknown | undefined>;
}
export interface BlockGroup {
    name: string;
    blocks?: Block[];
}
export interface Content {
    afterBlocks?: Block[];
    beforeBlocks?: Block[];
    extraBlocks?: BlockGroup[];
    blocks: Block[];
    lastUpdatedAt: string;
}
export declare const getMeta: (extraBlocks: BlockGroup[] | undefined) => Record<string, unknown> | undefined;
export declare const isBlock: (b: any) => b is Block;
export declare const isContent: (c: any) => c is Content;
