import { Content, Block } from './cms';
interface RenderMetaInfo {
    imports: Map<string, string>;
}
export declare class ContentDOM {
    protected content: Content;
    protected meta: RenderMetaInfo;
    constructor(content: Content);
    renderToString: () => string;
    protected renderImportsToString: () => string;
    protected renderBlocksToString: (blocks: Block[]) => string | undefined;
    protected renderBlockToString: (block: Block) => string;
    protected propsToString: (props?: any) => string;
}
export {};
