import { FC } from 'react';
interface Props {
    defaultLocale: string;
    locale: string;
    children: any;
}
declare const LocalizationContextProvider: FC<Props>;
export default LocalizationContextProvider;
