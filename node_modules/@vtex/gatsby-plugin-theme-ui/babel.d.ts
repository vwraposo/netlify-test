import { Visitor } from '@babel/traverse';
import BabelTypes from '@babel/types';
interface Babel {
    types: typeof BabelTypes;
}
interface Options {
    inFile: string;
    inPath: string;
}
declare const plugin: (_: Babel, options: Options) => {
    visitor: Visitor<{}>;
};
export default plugin;
