export interface State {
    state: 'emailForm' | 'emailForm.invalidEmailInput' | 'emailForm.authError' | 'accessCodeForm' | 'accessCodeForm.invalidAccessCode' | 'accessCodeForm.badAccessCode' | 'accessCodeForm.authError';
    email?: string;
}
export interface Action {
    type: State['state'];
    email?: string;
}
export declare const reducer: (state: State, action: Action) => State;
