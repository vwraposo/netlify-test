import { FC } from 'react';
import { State } from './state';
interface Props {
    variant: string;
    onSubmit: (email: string) => Promise<void>;
    onBack: () => void;
    state: State;
}
declare const AccessCodeForm: FC<Props>;
export default AccessCodeForm;
