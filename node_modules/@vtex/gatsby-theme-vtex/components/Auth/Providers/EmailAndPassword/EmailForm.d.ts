import { FC } from 'react';
import { State } from './state';
interface Props {
    variant: string;
    state: State;
    onBack: () => void;
    onSubmit: (email: string) => Promise<void>;
}
declare const EmailForm: FC<Props>;
export default EmailForm;
