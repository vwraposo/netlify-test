"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SearchSuggestionsList = void 0;
/** @jsx jsx */
const store_ui_1 = require("@vtex/store-ui");
exports.SearchSuggestionsList = ({ items, variant, children, }) => (<ul sx={{ variant: `suggestions.${variant}.list` }}>
    {items === null || items === void 0 ? void 0 : items.map((item, index) => (<li key={item.key}>
        {children({
    item,
    index,
    variant: `suggestions.${variant}.item`,
})}
      </li>))}
  </ul>);
