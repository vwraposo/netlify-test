/// <reference types="react" />
export interface BaseListItem {
    key: string;
}
interface ListItemProps<T extends BaseListItem> {
    item: T;
    index: number;
    variant: string;
}
interface Props<T extends BaseListItem> {
    variant: string;
    items: T[] | null | undefined;
    children: (props: ListItemProps<T>) => JSX.Element;
}
export declare const SearchSuggestionsList: <T extends BaseListItem>({ items, variant, children, }: Props<T>) => JSX.Element;
export {};
