import { FC } from 'react';
import { SearchQueryQuery } from './__generated__/SearchQuery.graphql';
interface Props {
    initialData: SearchQueryQuery | undefined;
    columns: number[];
    pageSize?: number;
}
declare const List: FC<Props>;
export declare const query: never;
export default List;
