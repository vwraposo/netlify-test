import { FC } from 'react';
export interface Props {
    variant?: string;
    isActive?: boolean | ((index: number) => boolean);
}
declare const SearchFilters: FC<Props>;
export default SearchFilters;
