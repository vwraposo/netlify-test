import { FC } from 'react';
import { Props as DesktopProps } from './Desktop';
interface Props extends DesktopProps {
    toggle: () => void;
    isOpen: boolean;
}
declare const SearchFilters: FC<Props>;
export default SearchFilters;
