import { FC } from 'react';
declare type Props = {
    skuId: string;
    seller: string;
    country: string;
    initialPostalCode?: string;
    variant: string;
    quantity: number;
};
declare const ShippingSimulatorWrapper: FC<Props>;
export default ShippingSimulatorWrapper;
