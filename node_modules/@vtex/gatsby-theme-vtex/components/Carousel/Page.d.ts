import { ComponentPropsWithoutRef, FC } from 'react';
import { ResponsivePicture } from '@vtex/store-ui';
declare type IResponsivePicture = ComponentPropsWithoutRef<typeof ResponsivePicture>;
export interface Item extends Omit<IResponsivePicture, 'variant' | 'loading'> {
    href: string;
}
interface Props {
    item: Item;
    loading: 'eager' | 'lazy';
    variant: string;
}
declare const CarouselPage: FC<Props>;
export default CarouselPage;
