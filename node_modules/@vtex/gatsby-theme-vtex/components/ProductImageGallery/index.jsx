"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const store_ui_1 = require("@vtex/store-ui");
const ArrowLeft_1 = __importDefault(require("./ArrowLeft"));
const ArrowRight_1 = __importDefault(require("./ArrowRight"));
const PaginationDots_1 = __importDefault(require("./PaginationDots"));
const Page_1 = __importDefault(require("./Page"));
const variant = 'productImageGallery';
const ProductImageGallery = ({ allItems, showArrows = true, showDots = true, autoplay, autoplayTimeout, }) => {
    const { page, items, totalPages, setPage, setNextPage, setPreviousPage, } = store_ui_1.useSlider({
        allItems,
        pageSize: 1,
        autoplay,
        autoplayTimeout,
    });
    // this is safe, since there is only one item per page
    const [item] = items;
    return (<store_ui_1.Box variant={variant}>
      <store_ui_1.Box variant={`${variant}.group`}>
        {showArrows && (<ArrowLeft_1.default variant={variant} onClick={() => setPreviousPage()}/>)}
        <Page_1.default variant={variant} item={item}/>
        {showArrows && (<ArrowRight_1.default variant={variant} onClick={() => setNextPage()}/>)}
      </store_ui_1.Box>
      {showDots && (<PaginationDots_1.default variant={variant} onSelect={setPage} selectedPage={page} totalPages={totalPages}/>)}
    </store_ui_1.Box>);
};
exports.default = ProductImageGallery;
