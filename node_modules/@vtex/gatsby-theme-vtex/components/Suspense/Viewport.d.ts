import { FC, SuspenseProps } from 'react';
interface Props extends SuspenseProps {
    preloader?: () => Promise<any>;
    rootMargin?: string;
    threshold?: number;
}
declare const SuspenseViewport: FC<Props>;
export default SuspenseViewport;
