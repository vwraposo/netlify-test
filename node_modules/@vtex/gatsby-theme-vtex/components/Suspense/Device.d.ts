import { FC, SuspenseProps } from 'react';
import { useDevice } from '../../sdk/media/useDevice';
declare type Device = ReturnType<typeof useDevice>;
interface Props extends SuspenseProps {
    device: Device;
}
declare const SuspenseDevice: FC<Props>;
export default SuspenseDevice;
