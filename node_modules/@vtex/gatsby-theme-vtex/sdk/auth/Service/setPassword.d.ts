interface Options {
    login: string;
    newPassword: string;
    currentPassword?: string;
    accesskey?: string;
    recaptcha?: string;
}
export declare const setPassword: ({ login, newPassword, currentPassword, accesskey, recaptcha, }: Options) => Promise<void>;
export {};
