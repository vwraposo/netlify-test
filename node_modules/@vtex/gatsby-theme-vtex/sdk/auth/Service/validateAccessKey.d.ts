interface Options {
    setPreference?: boolean;
    login: string;
    accessKey: string;
    recaptcha?: string;
}
export declare const validateAccessKey: ({ recaptcha, setPreference, accessKey, login, }: Options) => Promise<void>;
export {};
