interface Options {
    fingerprint?: string;
    callbackUrl?: string;
    user?: string;
    returnUrl: string;
}
export declare const startLogin: ({ returnUrl, callbackUrl, fingerprint, user, }: Options) => Promise<void>;
export {};
