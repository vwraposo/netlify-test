interface Options {
    login: string;
    password: string;
    recaptcha?: string;
    fingerprint?: string;
}
export declare const validatePassword: ({ login, password, recaptcha, fingerprint, }: Options) => Promise<void>;
export {};
