import { ConfigInterface } from 'swr';
import { RequestOptions } from './request';
export declare type QueryOptions = ConfigInterface & RequestOptions;
export declare const useQuery: <Query extends unknown = any, Variables extends unknown = any>(options: QueryOptions) => import("swr").responseInterface<Query, any[]>;
