import { CreateTextMaskConfig } from 'text-mask-core';
import { MutableRefObject } from 'react';
interface Args extends Omit<CreateTextMaskConfig, 'inputElement'> {
    value?: string;
    input: MutableRefObject<HTMLInputElement | null>;
    onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void;
}
export default function useMaskedInput({ guide, input, keepCharPositions, mask, onChange, pipe, placeholderChar, showMask, value, }: Args): (event: React.ChangeEvent<HTMLInputElement>) => void;
export {};
