import { Session } from './types';
export interface Options {
    stale?: boolean;
}
export declare type Action = {
    type: 'create';
} | {
    type: 'clear';
} | {
    type: 'patch';
    data: any;
};
export declare const useSession: (options?: Options | undefined) => [Session | null | undefined, (action: Action) => Promise<void>];
