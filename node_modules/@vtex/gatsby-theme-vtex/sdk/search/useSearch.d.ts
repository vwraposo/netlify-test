import { QueryOptions } from '../graphql/useQueryInfinite';
interface BaseQueryShape {
    vtex: {
        productSearch: Maybe<{
            products: Maybe<any[]>;
        }>;
    };
}
interface Options<Query extends BaseQueryShape | undefined> {
    query: QueryOptions;
    initialData: Query;
    pageSize?: number;
}
export declare const useSearch: <Query extends BaseQueryShape | undefined>({ query, initialData, pageSize, }: Options<Query>) => {
    fetchMore: () => Promise<Query[] | undefined>;
    isLoadingMore: boolean;
    isReachingEnd: boolean;
    data: Query[] | undefined;
    error: any;
};
export {};
