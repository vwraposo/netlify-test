"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useGalleryItems = void 0;
const react_1 = require("react");
exports.useGalleryItems = (images, videos, productName) => react_1.useMemo(() => {
    const imagesMapped = images.map((props) => ({
        type: 'image',
        props,
    }));
    const videosMapped = videos.map(({ videoUrl }) => ({
        type: 'video',
        props: { src: videoUrl, title: productName },
    }));
    return [...imagesMapped, ...videosMapped];
}, [images, videos]);
