import progress, { NProgressOptions } from 'nprogress';
interface SetupOptions extends Partial<NProgressOptions> {
    color: string;
}
export declare const setup: (options: SetupOptions) => progress.NProgress;
export declare type NProgress = typeof progress;
export {};
