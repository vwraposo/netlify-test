"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.throttle = void 0;
exports.throttle = (cb, wait = 166) => {
    let isThrottled = false;
    let lastReturn;
    const throttled = (...args) => {
        if (isThrottled && lastReturn !== undefined) {
            return lastReturn;
        }
        lastReturn = cb(args);
        if (!isThrottled) {
            isThrottled = true;
            setTimeout(() => {
                isThrottled = false;
            }, wait);
        }
        return lastReturn;
    };
    return throttled;
};
