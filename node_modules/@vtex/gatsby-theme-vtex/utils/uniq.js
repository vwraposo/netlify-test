"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.uniqBy = void 0;
exports.uniqBy = (array, fn) => {
    const seen = new Set();
    return array.reduce((acc, curr) => {
        const elem = fn(curr);
        if (!seen.has(elem)) {
            seen.add(elem);
            acc.push(curr);
        }
        return acc;
    }, []);
};
