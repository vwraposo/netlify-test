export { stringify, convertFromPath, validateRedirect, generateRewrites, generateRedirects, generatePathLocation, generateNginxConfiguration, };
export interface NginxDirective {
    cmd: string[];
    children?: NginxDirective[];
}
declare function generateNginxConfiguration(rewrites: Redirect[], redirects: Redirect[], headersMap: PathHeadersMap, files: string[], options: PluginOptions): string;
declare function stringify(directives: NginxDirective[]): string;
declare function convertFromPath(path: string): string;
declare function validateRedirect({ fromPath, toPath }: Redirect): boolean;
declare function generateRewrites(rewrites: Redirect[]): NginxDirective[];
declare function generateRedirects(redirects: Redirect[]): NginxDirective[];
declare function generatePathLocation(path: string, headers: Header[], files: string[], options: PluginOptions): NginxDirective | undefined;
