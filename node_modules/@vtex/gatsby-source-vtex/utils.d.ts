import { SourceNodesArgs } from 'gatsby';
import { Category, Channel } from './types';
export declare const createChannelNode: ({ actions: { createNode }, createNodeId, createContentDigest, }: SourceNodesArgs, { extraContext, ...channel }: Channel) => void;
export declare const createDepartmentNode: ({ actions: { createNode }, createNodeId, createContentDigest, }: SourceNodesArgs, { id, children, ...department }: Category) => void;
