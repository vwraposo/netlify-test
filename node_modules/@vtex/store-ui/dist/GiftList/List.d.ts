import { FunctionComponent } from 'react';
interface Props<T> {
    variant?: string;
    list: T[];
    ItemComponent: FunctionComponent<T>;
}
declare const GiftListList: <T extends JSX.IntrinsicAttributes>({ list, ItemComponent, variant, }: Props<T>) => JSX.Element;
export default GiftListList;
