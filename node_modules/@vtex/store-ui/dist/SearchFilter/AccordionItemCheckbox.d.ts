import { FC } from 'react';
export interface Item {
    key: string;
    name: string;
    value: string;
    quantity: number;
    selected: boolean;
}
interface Props {
    item: Item;
    variant: string;
    onClick: (item: Item) => void;
}
export declare const SearchFilterAccordionItemCheckbox: FC<Props>;
export {};
