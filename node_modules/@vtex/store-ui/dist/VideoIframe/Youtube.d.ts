import { FC, HTMLProps } from 'react';
export interface Props extends HTMLProps<HTMLIFrameElement> {
    variant: string;
    autoplay?: boolean;
    loop?: boolean;
    title: string;
    url: string;
}
export declare const getThumbUrl: (videoUrl: string) => Promise<string>;
declare const Youtube: FC<Props>;
export default Youtube;
