import { ReactElement, ReactNode } from 'react';
import Collapsible, { CollapsibleProps } from './Collapsible';
declare type Props = {
    renderIcon?: (isActive: boolean) => ReactNode;
    mode?: 'multiOpen' | 'singleOpen';
    variant?: string;
} & {
    children: Array<ReactElement<CollapsibleProps>>;
};
declare function Accordion({ children, renderIcon, mode, variant, }: Props): JSX.Element;
declare namespace Accordion {
    var Section: typeof Collapsible;
}
export default Accordion;
