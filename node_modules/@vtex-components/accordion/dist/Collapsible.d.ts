import { ReactNode, PropsWithChildren } from 'react';
interface Props {
    header: ReactNode;
    variant?: string;
    id?: string;
    onClick?: (id: string) => void;
    isActive?: boolean;
    renderIcon?: (isActive: boolean) => ReactNode;
}
export declare type CollapsibleProps = PropsWithChildren<Props>;
declare function Collapsible({ id, header, children, isActive, onClick, renderIcon, variant, }: CollapsibleProps): JSX.Element;
export default Collapsible;
