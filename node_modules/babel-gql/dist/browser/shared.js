"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.combinedIds = void 0;
function combinedIds(ids) {
    var _a = __read(ids), first = _a[0], rest = _a.slice(1);
    if (rest.length === 0) {
        return first;
    }
    var chunkSize = Math.floor(first.length / ids.length);
    return (first.slice(0, chunkSize) +
        rest
            .sort()
            .map(function (id) { return id.slice(0, chunkSize); })
            .join(""));
}
exports.combinedIds = combinedIds;
//# sourceMappingURL=shared.js.map